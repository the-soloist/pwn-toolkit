#!/usr/bin/env python
import argparse

parser = argparse.ArgumentParser()
parser.add_argument("filepath", action="store")
parser.add_argument("-l", "--patch-libc", action="store_ture")
parser.add_argument("-a", "--patch-all", action="store_ture")
parser.add_argument("-b", "--backup", action="store_ture")
args = parser.parse_args()


def run(args):
    pass


if __name__ == "__main__":
    run(args)
